version: '3.9'

services:
  spi:
    build: .
    ports:
      - '5000:5000'
    environment:
      PORT: '5000'
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:5000/healthz']
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
